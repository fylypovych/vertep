# Agents

Channel agents are adapters that submit normalized jobs to CORE. Telegram is implemented through long polling (`getUpdates`) with the `/api/telegram/webhook` endpoint retained for backward compatibility.
