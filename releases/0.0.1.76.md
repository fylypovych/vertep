# Vertep 0.0.1.76

Job UI (#23): структурований timeline (`event_log` з `JobEvent`) показується незалежно від legacy `events` масиву; `structuredEvents()` передає `attempt`, `worker`, `task`, `artifact`, `error` контекст; legacy fallback вилучено.

Fleet/Queue (#25, #26, #28): worker restart/update пов'язані з реальним consumer у `worker/service.py` (обработка `RESTARTING` desired_state, перевірка статусу перед claim); dispatcher gating по `tested_capabilities` без fallback до `declared`; `DISABLED/RESTARTING/REVOKED` блокують нові claims.

Domain clients migration (#25): мigiровано fleet, workers, queue, publishing, system endpoints на типізовані DTO (`MetricsResponse`, `IntegrationStatusResponse`, `BackupListResponseDTO`, `NodeActionResponse`, `NodeRevokeResponse` тощо); видалено `Record<string, unknown>` для цих доменів.

Key lifecycle (#27): OpenSSL signing flow виправлено (TypeError у `generate-root-metadata.py`); всі 4 тести `test_key_lifecycle.py` проходять; rotation/recovery/tamper/expiry/revocation/channel/downgrade acceptance пройдено.

Release qualification (#27): `qualify-release.py` перевіряє cross-artifact consistency (version/SHA/digests/signatures між images, manifest, SBOM, checksums, bundle).

Infrastructure qualification (#33): `qualify_infrastructure.py` S01–S08 scenarios catalog, pytest integration, JSON proof structure.

CORE Generation Gate (#31): `finalize_job` використовує `providers.video_engine().render()` замість прямих `AssemblyProvider` викликів; VideoEngine обертає native FFmpeg або диспатчить remote engines.

Всі автоматизовані тести: 792 passed, 54 skipped; web-v2 build успіх.