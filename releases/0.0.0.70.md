# Vertep 0.0.0.70

Дата випуску: 2026-09-02

## Зміни

- Виправлено аварійне завершення оновлення й rollback на незмінному спільному `node_roles.json`: updater тепер розпізнає той самий inode та не намагається копіювати файл у самого себе.
- Невдала операція оновлення тепер повертає systemd ненульовий код завершення, тому реальний збій видно у стані служби, а не як оманливе `status=0`.

## Перевірка

- `186 passed, 9 skipped in 111.49s (0:01:51)`
