# Vertep 0.0.1.95

## Task 1 — Actor/chat/job access for content callbacks
- `vid_ok` та `vid_reject` перевіряють chat_id проти job.source перед виконанням.
- Чужий chat отримує "Доступ заборонено: chat не є власником job".
- `vid_edit` та `vid_regen` не блокуються actor check (будь-який admin може правити/перегенерувати).

## Task 2 — Polling durability
- `_interruptible_sleep` для 429 rate-limit та retry delay — `stop()` перериває sleep через 0.5s polling.
- Negative offset: завантажується без crash.
- Corrupted offset file: fallback на offset=0.
- Idempotent callback:同一 update_id не обробляється двічі після restart (offset зберігається ДО self.offset).
- Тести: 7 нових coverage tests.
