# Vertep 0.0.1.6

- Виправлено монтування Web UI v2 у `core/app.py`: v2-маунт тепер виконується лише за наявності скомпільованої збірки Angular (`web-v2/dist`); інакше з кореня сервується класична v1. Це усуває падіння імпорту `core.app` та помилку збирання тестів у CI, де немає скомпільованого фронтенду (раніше `web-v2/dist/` було закомічено в git, що приховувало проблему; після виключення build-артефактів з трекінгу імпорт падав з `RuntimeError: Directory 'web-v2/dist/vertep-admin-v2' does not exist`).
