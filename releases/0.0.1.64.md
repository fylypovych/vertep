# Vertep 0.0.1.64

## Мета
Закриття залишкового пробілу Issue #36 (i.0.0.0.33) та виправлення регресійного тесту.

## Зміни
- Issue #36 (i.0.0.0.33, #6): Web UI сторінки Job тепер рендерить самі scene preview зображення розкадровки inline через `<img>` (`web-v2/src/app/jobs/job-detail.component.ts`, хелпер `previewImageUrl()`). Раніше показувались лише artifact ID та посилання «Відкрити превʼю».
- Розширено Playwright-тест `tests/test_browser_e2e.py::test_storyboard_review_with_artifacts_and_approve`: перевіряється кількість inline-зображень сцен і коректний `src` (artifact download endpoint).
- Виправлено `tests/test_release_qualification.py::test_role_isolation_failure_is_reported`: харнесс JSON-обізнаний після переформатування `config/node_roles.json` у багаторядковий JSON у 0.0.1.60.

## Перевірка
- Angular production build: OK.
- pytest (non-browser): 598 passed, 14 skipped (наявні Windows-скопи).
- Browser E2E: потребують запущеного сервера (допустима невдача за AGENTS.md).
- `git diff --check`: без проблем.