# Vertep 0.0.0.71

Дата випуску: 2026-09-02

## Зміни

- Оновлення більше не намагається змінювати `/usr/local/bin/vertep` усередині захищеної systemd-служби з `ProtectSystem=strict`; постійний symlink залишається відповідальністю Bootstrap, а host-скрипт оновлюється у `/opt/vertep/scripts`.
- `vertep status` тепер показує останню змістовну помилку замість багатосторінкового журналу Docker і декодує службові HTML-послідовності на кшталт `&#x20;`.

## Перевірка

- `187 passed, 9 skipped in 91.04s (0:01:31)`
