# Vertep 0.0.1.87

## Real Test Runner (#66)
- Заміна `gh` CLI на GitHub REST API (`urllib.request`) — жодних зовнішніх залежностей
- Повний 40-символьний SHA deployment identity (пріоритет: `GITHUB_SHA` → `git rev-parse HEAD` → `VERSION` → `"unknown"`)
- 9 нових health check probes: `postgres_tcp`, `redis_tcp`, `ollama_probe`, `comfyui_probe`, `gpu`, `tts`, `publisher`, `backup`, `monitoring`
- Розширений secret redaction: JSON key-value patterns, credentials, access_key, client_secret, jwt_secret, encryption_key, internal_api_key
- `retry_report` зберігає version/commit_sha в `github_report`
- `_already_reported` пробрасує transient errors замість поглинання

## Storyboard Approval (#66)
- Version-bound approval: `approve()` приймає `expected_image_version` для захисту від stale approvals
- Concurrent revision guards: блокування `sb_edit`, `sb_img_edit`, `sc_edit`, `vid_edit` при наявності pending revision
- Telegram Admin Chat: всі destructive callbacks перевіряють `is_admin_chat(chat_id)`
- Publication Guard: `_publish_to_channel` перевіряє `approval_status == "approved"` перед публікацією
- Video Regeneration: використовує `_collect_active_storyboard_images()` замість glob по всіх versions

## Telegram & Polling (#66)
- Polling Offset: `_save_offset_data` зберігає offset на диск ДО оновлення in-memory offset

## Bootstrap (#66)
- Docker/Compose version verification після встановлення
- `jq` додано до Dockerfile

## Web UI (#66)
- Вибір історичних storyboard versions через dropdown
- `active_image_version` в Job model
- Approve storyboard передає `image_version` для version-bound approval

## Storyboard Task Lifecycle (#64)
- Claim переходи QUEUED → GENERATING (синхронізація repository record та Job lifecycle)
- Rejected results НЕ звільняють worker (task залишається в flight для легітимного claim holder)
- `regenerate_script` зберігає revision request (без цього regenerated script ігнорує feedback)

## Operations (#64)
- Видалено unreachable `return operation` після early return

## Тести
- 825 passed (0 failed)
- 5 generation gate passed
- 8 тестів `TestSecretRedaction` (redaction patterns, JSON, mixed content)
- 5 тестів `TestBootstrapDockerCompose` (Docker/Compose version checks)
