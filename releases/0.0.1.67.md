# Vertep 0.0.1.67

## Мета
Фікс тесту key-lifecycle: tampered metadata тепер перепідписується, щоб валідація досягала перевірки полів (revoked/channel), а не втрачалась на threshold підписів.

## Зміни
- `tests/test_key_lifecycle.py`: додано хелпер `resign()` для перепідпису змінених метаданих приватними ключами; оновлено `test_root_metadata_fail_closed_security_cases` — тепер перевіряє `revoked` та `wrong_channel` через коректно перепідписані структури.
- 3/4 тестів key-lifecycle тепер прходять на Linux CI; `test_job_workspace_contracts.py::test_job_list_status_exact_filter` — незалежний flaky, не від змін.

## Перевірка
- `python -m compileall -q scripts/generate-root-metadata.py tests/test_key_lifecycle.py`: OK.
- `pytest --collect-only tests/test_key_lifecycle.py`: 4 tests collected.
- Повна верифікація openssl-тестів виконується на Linux CI у `prepare`-вороті release workflow.