# Vertep 0.0.0.67

Дата випуску: 2026-09-02

## Зміни

- Команди `vertep start`, `vertep stop` і `vertep restart` тепер завжди показують зрозумілий український результат виконання; при HTTP 423 команда оновлення пояснює блокування та підказує `sudo vertep recover`.
- Отримання маніфесту оновлення тепер автоматично повторюється до трьох разів із довшим тайм-аутом, а остаточна мережева помилка містить назву етапу й кількість спроб.
- Релізний скрипт більше не додає до коміту сторонні невідстежувані файли з робочої теки.

## Перевірка

- `185 passed, 9 skipped in 79.60s (0:01:19)`
